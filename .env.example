# --- Auth (required) ---

# Shared passcode that gates the entire console. Pick something long and random —
# there's no per-person login, this is the only thing standing between the internet
# and this data.
FOUNDER_PASSCODE=

# Random secret used to sign the session cookie (HMAC-SHA256). Generate with:
#   openssl rand -hex 32
SESSION_SECRET=
